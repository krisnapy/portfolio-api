rc: ./.lefthookrc

pre-commit:
  piped: true
  commands:
    1-linting:
      glob: '*.{js,ts,tsx}'
      run: bun run lint && bun run lint:fix
    2-formatting:
      glob: '*.{js,ts,tsx,css,scss,md,json}'
      run: bun run prettier && bun run prettier:fix
