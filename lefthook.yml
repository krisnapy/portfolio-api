rc: ./.lefthookrc

pre-commit:
  piped: true
  commands:
    1-linting:
      glob: '*.{js,ts,tsx}'
      run: bun run eslint {staged_files}
    2-formatting:
      glob: '*.{js,ts,tsx,css,scss,md,json}'
      run: bun run prettier {staged_files}
